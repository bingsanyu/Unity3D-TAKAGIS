using UnityEngine;
using System.Collections;

public class cell_dis : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {
        Destroy(gameObject, 5);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
